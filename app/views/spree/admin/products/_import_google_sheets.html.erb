<%# Custom "Import from Google Sheets" button, injected into the products index page-actions
    via Spree.admin.partials.products_actions (Spree 5.2 extension point). %>
<%= link_to_with_icon 'file-import', Spree.t('admin.product_import.button'), spree.new_admin_product_import_path, class: 'btn btn-light' if can?(:create, Spree::Product) %>
